#include <stdio.h>
#include "main.h"

/**
 * main - Prints the name
 * @argc: count of present arguments
 * @argv: argument vector
 * Return: Always 0.
 */
int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);

	return (0);
}
