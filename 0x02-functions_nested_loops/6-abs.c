#include "main.h"
/**
  * _abs - Does the absolute value of an integer
  * @1: integer input number
  * Return: absolute value
  */
int _abs(int i)
{
	if (i >= 0)
	{
		return(i);
	}
	else
	{
		return (i * -1);
	}
}
